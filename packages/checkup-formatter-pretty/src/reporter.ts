import React, { FC } from 'react';
import { render, Box, Text } from 'ink';
import { componentMaps } from './components';

const Reporter = (componentName: string) => {
  const component = componentMaps;
};
