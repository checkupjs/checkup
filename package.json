{
  "name": "checkup",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "https://github.com/checkupjs/checkup.git",
  "author": "Steve Calvert <steve.calvert@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "echo 'Please use `yarn wsrun --stages build` to build all workspace packages in dependency order.'",
    "build:watch": "echo 'Please use `yarn wsrun watch` to run the build in watch mode for each individual package.'",
    "test": "yarn workspaces run test",
    "lint": "eslint . --cache --ext .ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.20.0",
    "@typescript-eslint/parser": "^2.20.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.0",
    "eslint-plugin-node": "^11.0.0",
    "eslint-plugin-prettier": "^3.1.2",
    "husky": "^4.2.1",
    "lint-staged": "^10.0.7",
    "prettier": "^1.19.1",
    "wsrun": "^5.2.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "packages/**/*.ts": [
      "yarn lint"
    ]
  }
}
