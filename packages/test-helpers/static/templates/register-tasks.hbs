
{{#each classes as |class|}}
{{{class}}}
{{/each}}

const hook = async function({ context, tasks }) {
  {{#each taskNames as |taskName|}}
  tasks.registerTask(new {{taskName}}(context));
  {{/each}}
};

exports.default = hook;