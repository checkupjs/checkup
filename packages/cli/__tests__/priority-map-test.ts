describe('PriorityMap', () => {
  it('can create an instance of a PriorityMap', () => {});
  it('set will set an entry based on priority', () => {});
  it('get will return an entry based on priority', () => {});
  it('has will correctly identify an item', () => {});
  it('size will return the correct total size across maps', () => {});
});
