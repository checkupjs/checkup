import { BaseTaskResult, ui } from '@checkup/core';

<%_ const resultClass = `${_.upperFirst(_.camelCase(name))}TaskResult` _%>
export default class <%- resultClass %> extends BaseTaskResult {
  stdout() {
    ui.styledHeader(this.meta.taskName);
  }

  json() {
    return {
      meta: this.meta,
      result: {},
    };
  }

  pdf() {}
}
